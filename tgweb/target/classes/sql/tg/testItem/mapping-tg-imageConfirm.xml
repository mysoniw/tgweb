<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE queryservice PUBLIC "-//ANYFRAME//DTD QUERYSERVICE//EN" 
	"http://www.anyframejava.org/dtd/anyframe-core-query-mapping-3.2.dtd">
<queryservice>
	<queries>
		<query id="ImageConfirmMainDao.findAll" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT A.*
FROM (
	SELECT
		ROW_NUMBER() OVER(ORDER BY MAX(A.CATEGORY), MAX(A.MODEL), MAX(A.FW_VERSION) DESC) AS ROWNUM
		, COUNT(1) OVER() AS TOTALCOUNT
		, MAX(TC_IDX) AS TC_IDX
		, MAX(A.CATEGORY) AS CATEGORY
		, MAX(A.MODEL) AS MODEL
		, MAX(A.FW_VERSION) AS FW_VERSION
		, MAX(A.TEST_PROJECT_ID) AS TEST_PROJECT_ID
		, MAX(A.TEST_SUITE_ID) AS TEST_SUITE_ID
		, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
		, COUNT(TOTAL_CYCLE) AS TEST_CASE_COUNT
		, COUNT(TOTAL_CYCLE) - SUM(RESULT_INT) - SUM(NG_INT) AS TEST_CASE_PASS_COUNT
		, SUM(RESULT_INT) AS TEST_CASE_FAIL_COUNT
		, SUM(A.NG_INT) AS NG
		, CAST(ROUND((COUNT(TOTAL_CYCLE) - SUM(RESULT_INT) - SUM(NG_INT)) / CAST(COUNT(TOTAL_CYCLE) AS FLOAT) * 100, 2) AS VARCHAR) + '%' AS PASS_RATE
		, MAX(EXTRA3) AS EXTRA3
	FROM
		(
		SELECT
			MAX(TC_IDX) AS TC_IDX
			, MAX(A.CATEGORY) AS CATEGORY
			, MAX(A.MODEL) AS MODEL
			, MAX(A.FW_VERSION) AS FW_VERSION
			, MAX(A.TEST_PROJECT_ID) AS TEST_PROJECT_ID
			, MAX(A.TEST_SUITE_ID) AS TEST_SUITE_ID
			, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
			, CASE  
				WHEN SUM(A.RESULT_INT) > 0 THEN 1
				ELSE 0
			END	AS RESULT_INT
			, CASE 
				WHEN (SUM(A.RESULT_INT) = 0 OR SUM(A.RESULT_INT) IS NULL) AND SUM(A.NG_INT) > 0 THEN 1
				ELSE 0
			END AS NG_INT
			, COUNT(A.TEST_CASE_ID) AS TOTAL_CYCLE
			, MAX(EXTRA3) AS EXTRA3
		FROM
			(
			SELECT
				A.TC_IDX
				, A.CATEGORY
				, A.MODEL
				, A.FW_VERSION
				, CASE RESULT 
					WHEN 'PASS' THEN 0
					WHEN 'FAIL' THEN 1
					ELSE 0
				END AS RESULT_INT
				, CASE RESULT 
					WHEN 'NG' THEN 1
					ELSE 0
				END AS NG_INT
				, TEST_PROJECT_ID
				, TEST_SUITE_ID
				, TEST_CASE_ID
				, TEST_CASE_NAME
				, SUITE_DESCRIPTION
				, EXTRA3
			FROM
				SQE_TEST_CASE_MASTER A LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
			WHERE 
				TEST_PROJECT_ID = 'IMAGE_CONFIRM'
				AND B.RESULT <> 'Unknown'
			#if ($CATEGORY && $CATEGORY != "" && $CATEGORY != "null")
				AND A.CATEGORY = :CATEGORY
			#end
			#if ($MODEL && $MODEL != "" && $MODEL != "null")
				AND A.MODEL = :MODEL
			#end
			#if ($FW_VERSION && $FW_VERSION != "" && $FW_VERSION != "null")
				AND A.FW_VERSION = :FW_VERSION
			#end
			#if ($TEST_SUITE_ID && $TEST_SUITE_ID != "" && $TEST_SUITE_ID != "null")
				AND A.TEST_SUITE_ID = :TEST_SUITE_ID
			#end
		) A
		WHERE 
			1 = 1
		GROUP BY
			A.CATEGORY, A.MODEL, A.FW_VERSION, A.TEST_SUITE_ID, A.TEST_CASE_ID
	) A
	WHERE
		1 = 1
	GROUP BY
		A.CATEGORY, A.MODEL, A.FW_VERSION, A.TEST_SUITE_ID
) A
WHERE 
	1 = 1
#if (!$loadOnce || $loadOnce == "" || $loadOnce == "null")
	AND A.ROWNUM BETWEEN (:rows * (:page - 1)) + 1 AND (:rows * :page)
#end
ORDER BY "{{sidx}}"
#if ($sord == "desc")
	DESC
#else
	ASC
#end
				]]>
			</statement>
		</query>
		<query id="ImageConfirmMainDao.exportExcel" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT
	MAX(A.TEST_PROJECT_ID) AS "TEST_PROJECT_ID"
	, MAX(A.CATEGORY) AS "CATEGORY"
	, MAX(A.MODEL) AS "MODEL"
	, MAX(A.FW_VERSION) AS "FW_VERSION"
	, MAX(A.TEST_SUITE_ID) AS "TEST_SUITE_ID"
	, MAX(A.SUITE_DESCRIPTION) AS "SUITE_DESCRIPTION"
	, COUNT(TOTAL_CYCLE) AS "TOTAL_TC"
	, COUNT(TOTAL_CYCLE) - SUM(RESULT_INT) - SUM(NG_INT) AS "PASS"
	, SUM(RESULT_INT) AS "FAIL"
	, SUM(A.NG_INT) AS "NG"
	, CAST(ROUND((COUNT(TOTAL_CYCLE) - SUM(RESULT_INT) - SUM(NG_INT)) / CAST(COUNT(TOTAL_CYCLE) AS FLOAT) * 100, 2) AS VARCHAR) + '%' AS "PASS_RATE"
FROM
	(
	SELECT
		MAX(TC_IDX) AS TC_IDX
		, MAX(A.CATEGORY) AS CATEGORY
		, MAX(A.MODEL) AS MODEL
		, MAX(A.FW_VERSION) AS FW_VERSION
		, MAX(A.TEST_PROJECT_ID) AS TEST_PROJECT_ID
		, MAX(A.TEST_SUITE_ID) AS TEST_SUITE_ID
		, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
		, CASE  
			WHEN SUM(A.RESULT_INT) > 0 THEN 1
			ELSE 0
		END	AS RESULT_INT
		, CASE 
			WHEN (SUM(A.RESULT_INT) = 0 OR SUM(A.RESULT_INT) IS NULL) AND SUM(A.NG_INT) > 0 THEN 1
			ELSE 0
		END AS NG_INT
		, COUNT(A.TEST_CASE_ID) AS TOTAL_CYCLE
	FROM
		(
		SELECT
			A.TC_IDX
			, A.CATEGORY
			, A.MODEL
			, A.FW_VERSION
			, CASE RESULT 
				WHEN 'PASS' THEN 0
				WHEN 'FAIL' THEN 1
				ELSE 0
			END AS RESULT_INT
			, CASE RESULT 
				WHEN 'NG' THEN 1
				ELSE 0
			END AS NG_INT
			, TEST_PROJECT_ID
			, TEST_SUITE_ID
			, TEST_CASE_ID
			, TEST_CASE_NAME
			, SUITE_DESCRIPTION
		FROM
			SQE_TEST_CASE_MASTER A LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
		WHERE 
			TEST_PROJECT_ID = 'IMAGE_CONFIRM'
			AND B.RESULT <> 'Unknown'
		#if ($CATEGORY && $CATEGORY != "" && $CATEGORY != "null")
			AND A.CATEGORY = :CATEGORY
		#end
		#if ($MODEL && $MODEL != "" && $MODEL != "null")
			AND A.MODEL = :MODEL
		#end
		#if ($FW_VERSION && $FW_VERSION != "" && $FW_VERSION != "null")
			AND A.FW_VERSION = :FW_VERSION
		#end
		#if ($TEST_SUITE_ID && $TEST_SUITE_ID != "" && $TEST_SUITE_ID != "null")
			AND A.TEST_SUITE_ID = :TEST_SUITE_ID
		#end
	) A
	WHERE 
		1 = 1
	GROUP BY
		A.CATEGORY, A.MODEL, A.FW_VERSION, A.TEST_SUITE_ID, A.TEST_CASE_ID
) A
WHERE
	1 = 1
GROUP BY
	A.CATEGORY, A.MODEL, A.FW_VERSION, A.TEST_SUITE_ID
ORDER BY 
	MAX(A.CATEGORY), MAX(A.MODEL), MAX(A.FW_VERSION) DESC
				]]>
			</statement>
		</query>
		<query id="ImageConfirmTcDao.findAll" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT A.*
FROM (
	SELECT
		ROW_NUMBER() OVER(ORDER BY 
			#if ($filterColumn && $filterColumn != "" && $filterColumn != "null")
				MAX(A.RESULT_INT) DESC ,
			#end
				MAX(A.CATEGORY)
				, MAX(A.MODEL)
				, MAX(A.FW_VERSION) DESC
				, MAX(A.TEST_CASE_ID)
		) AS ROWNUM
		, COUNT(1) OVER() AS TOTALCOUNT
		, MAX(A.TC_IDX) AS TC_IDX
		, MAX(A.CATEGORY) AS CATEGORY
		, MAX(A.MODEL) AS MODEL
		, MAX(A.FW_VERSION) AS FW_VERSION
		, MAX(A.TEST_PROJECT_ID) AS TEST_PROJECT_ID
		, MAX(A.TEST_SUITE_ID) AS TEST_SUITE_ID
		, A.TEST_CASE_ID
		, MAX(A.TEST_CASE_NAME) AS TEST_CASE_NAME
		, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
		, CASE 
			WHEN SUM(A.RESULT_INT) > 0 THEN 'FAIL'
			WHEN (SUM(A.RESULT_INT) = 0 OR SUM(A.RESULT_INT) IS NULL) AND SUM(A.NG_INT) > 0 THEN 'NG'
			ELSE 'PASS' 
		END AS RESULT
		, COUNT(A.TEST_CASE_ID) - SUM(A.RESULT_INT) - SUM(A.NG_INT) AS PASS
		, SUM(A.RESULT_INT) AS FAIL
		, SUM(A.NG_INT) AS NG
		, COUNT(A.TEST_CASE_ID) AS TOTAL_CYCLE
		, CAST(ROUND((COUNT(A.TEST_CASE_ID) - SUM(A.RESULT_INT) - SUM(A.NG_INT)) / CAST(COUNT(A.TEST_CASE_ID) AS FLOAT) * 100, 2) AS VARCHAR) + '%' AS PASS_RATE
		, MAX(A.NODE_MAC) AS NODE_MAC
		, MAX(A.NODE_IP) AS NODE_IP
		, ISNULL(MAX(A.EXTRA1), '-') AS EXTRA1
		, ISNULL(MAX(A.EXTRA2), '-') AS EXTRA2
		, ISNULL(MAX(A.EXTRA3), '-') AS EXTRA3
		, ISNULL(MAX(A.EXTRA4), '-') AS EXTRA4
		, ISNULL(MAX(A.EXTRA5), '-') AS EXTRA5
		, ISNULL(MAX(A.EXTRA6), '-') AS EXTRA6
		, ISNULL(MAX(A.EXTRA7), '-') AS EXTRA7
		, ISNULL(MAX(A.EXTRA8), '-') AS EXTRA8
		, ISNULL(MAX(A.EXTRA9), '-') AS EXTRA9
		, ISNULL(MAX(A.EXTRA10), '-') AS EXTRA10
		, ISNULL(MAX(A.EXTRA11), '-') AS EXTRA11
		, ISNULL(MAX(A.EXTRA12), '-') AS EXTRA12
		, ISNULL(MAX(A.EXTRA13), '-') AS EXTRA13
		, ISNULL(MAX(A.EXTRA14), '-') AS EXTRA14
		, ISNULL(MAX(A.EXTRA15), '-') AS EXTRA15
	FROM
		(
		SELECT
			A.TC_IDX
			, A.CATEGORY
			, A.MODEL
			, A.FW_VERSION
			, CASE RESULT 
				WHEN 'PASS' THEN 0
				WHEN 'FAIL' THEN 1
				ELSE 0
			END AS RESULT_INT
			, CASE RESULT 
				WHEN 'NG' THEN 1
				ELSE 0
			END AS NG_INT
			, TEST_PROJECT_ID
			, TEST_SUITE_ID
			, TEST_CASE_ID
			, TEST_CASE_NAME
			, SUITE_DESCRIPTION
			, NODE_MAC
			, NODE_IP
			, EXTRA1
			, EXTRA2
			, EXTRA3
			, EXTRA4
			, EXTRA5
			, EXTRA6
			, EXTRA7
			, EXTRA8
			, EXTRA9
			, EXTRA10
			, EXTRA11
			, EXTRA12
			, EXTRA13
			, EXTRA14
			, EXTRA15
		FROM
			SQE_TEST_CASE_MASTER A LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
		WHERE 
			TEST_PROJECT_ID = 'IMAGE_CONFIRM'
			AND B.RESULT <> 'Unknown'
			AND A.TEST_SUITE_ID = :TEST_SUITE_ID
			AND A.CATEGORY = :CATEGORY
			AND A.MODEL = :MODEL
			AND A.FW_VERSION = :FW_VERSION
		#if ($EXTRA2 && $EXTRA2 != "" && $EXTRA2 != "null")
			AND B.EXTRA2 = :EXTRA2
		#end
		#if ($EXTRA3 && $EXTRA3 != "" && $EXTRA3 != "null")
			AND B.EXTRA3 = :EXTRA3
		#end
		#if ($EXTRA4 && $EXTRA4 != "" && $EXTRA4 != "null")
			AND B.EXTRA4 = :EXTRA4
		#end
		#if ($EXTRA5 && $EXTRA5 != "" && $EXTRA5 != "null")
			AND B.EXTRA5 = :EXTRA5
		#end
		#if ($EXTRA6 && $EXTRA6 != "" && $EXTRA6 != "null")
			AND B.EXTRA6 = :EXTRA6
		#end
		#if ($EXTRA7 && $EXTRA7 != "" && $EXTRA7 != "null")
			AND B.EXTRA7 = :EXTRA7
		#end
		#if ($EXTRA8 && $EXTRA8 != "" && $EXTRA8 != "null")
			AND B.EXTRA8 = :EXTRA8
		#end
		#if ($EXTRA9 && $EXTRA9 != "" && $EXTRA9 != "null")
			AND B.EXTRA9 = :EXTRA9
		#end
	) A
	WHERE 
		1 = 1
	GROUP BY
		A.TEST_CASE_ID
) A
WHERE 
	1 = 1
#if (!$loadOnce || $loadOnce == "" || $loadOnce == "null")
	AND A.ROWNUM BETWEEN (:rows * (:page - 1)) + 1 AND (:rows * :page)
#end
ORDER BY "{{sidx}}"
#if ($sord == "desc")
	DESC
#else
	ASC
#end
				]]>
			</statement>
		</query>
		<query id="ImageConfirmTcDao.exportExcel" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT
	MAX(A.TEST_PROJECT_ID) AS "TEST_PROJECT_ID"
	, MAX(A.CATEGORY) AS "CATEGORY"
	, MAX(A.MODEL) AS "MODEL"
	, MAX(A.FW_VERSION) AS "FW_VERSION"
	, MAX(A.TEST_SUITE_ID) AS "TEST_SUITE_ID"
	, MAX(A.TEST_CASE_NAME) AS "TEST_CASE_NAME"
	, MAX(A.SUITE_DESCRIPTION) AS "SUITE_DESCRIPTION"
#if ($CATEGORY == "NCAM")
	, ISNULL(MAX(A.EXTRA1), '-') AS "EXTRA1"
	, ISNULL(MAX(A.EXTRA2), '-') AS "EXTRA2"
	, ISNULL(MAX(A.EXTRA3), '-') AS "EXTRA3"
	, ISNULL(MAX(A.EXTRA4), '-') AS "EXTRA4"
	, ISNULL(MAX(A.EXTRA5), '-') AS "EXTRA5"
	, ISNULL(MAX(A.EXTRA6), '-') AS "EXTRA6"
	, ISNULL(MAX(A.EXTRA7), '-') AS "EXTRA7"
	, ISNULL(MAX(A.EXTRA8), '-') AS "EXTRA8"
	, ISNULL(MAX(A.EXTRA9), '-') AS "EXTRA9"
	, ISNULL(MAX(A.EXTRA10), '-') AS "EXTRA10"
	, ISNULL(MAX(A.EXTRA11), '-') AS "EXTRA11"
	, ISNULL(MAX(A.EXTRA12), '-') AS "EXTRA12"
#end
#if ($CATEGORY == "NVR")
	, ISNULL(MAX(A.EXTRA1), '-') AS "EXTRA1"
	, ISNULL(MAX(A.EXTRA3), '-') AS "EXTRA3"
	, ISNULL(MAX(A.EXTRA4), '-') AS "EXTRA4"
	, ISNULL(MAX(A.EXTRA5), '-') AS "EXTRA5"
#end
#if ($CATEGORY == "DVR")
	, ISNULL(MAX(A.EXTRA1), '-') AS "EXTRA1"
	, ISNULL(MAX(A.EXTRA2), '-') AS "EXTRA2"
	, ISNULL(MAX(A.EXTRA4), '-') AS "EXTRA4"
#end
	, ISNULL(MAX(A.EXTRA13), '-') AS "EXTRA13"
	, ISNULL(MAX(A.EXTRA14), '-') AS "EXTRA14"
	, ISNULL(MAX(A.EXTRA15), '-') AS "EXTRA15"
	, COUNT(A.TEST_CASE_ID) AS "TOTAL_CYCLE"
	, CASE 
		WHEN SUM(A.RESULT_INT) > 0 THEN 'FAIL'
		WHEN (SUM(A.RESULT_INT) = 0 OR SUM(A.RESULT_INT) IS NULL) AND SUM(A.NG_INT) > 0 THEN 'NG'
		ELSE 'PASS' 
	END AS "RESULT"
	, COUNT(A.TEST_CASE_ID) - SUM(A.RESULT_INT) - SUM(A.NG_INT) AS "PASS"
	, SUM(A.RESULT_INT) AS "FAIL"
	, SUM(A.NG_INT) AS "NG"
	, CAST(ROUND((COUNT(A.TEST_CASE_ID) - SUM(A.RESULT_INT) - SUM(A.NG_INT)) / CAST(COUNT(A.TEST_CASE_ID) AS FLOAT) * 100, 2) AS VARCHAR) + '%' AS "PASS_RATE"
FROM
	(
	SELECT
		A.TC_IDX
		, A.CATEGORY
		, A.MODEL
		, A.FW_VERSION
		, CASE RESULT 
			WHEN 'PASS' THEN 0
			WHEN 'FAIL' THEN 1
			ELSE 0
		END AS RESULT_INT
		, CASE RESULT 
			WHEN 'NG' THEN 1
			ELSE 0
		END AS NG_INT
		, TEST_PROJECT_ID
		, TEST_SUITE_ID
		, TEST_CASE_ID
		, TEST_CASE_NAME
		, SUITE_DESCRIPTION
		, NODE_MAC
		, NODE_IP
		, EXTRA1
		, EXTRA2
		, EXTRA3
		, EXTRA4
		, EXTRA5
		, EXTRA6
		, EXTRA7
		, EXTRA8
		, EXTRA9
		, EXTRA10
		, EXTRA11
		, EXTRA12
		, EXTRA13
		, EXTRA14
		, EXTRA15
	FROM
		SQE_TEST_CASE_MASTER A LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
	WHERE 
		TEST_PROJECT_ID = 'IMAGE_CONFIRM'
		AND B.RESULT <> 'Unknown'
		AND A.CATEGORY = :CATEGORY
		AND A.MODEL = :MODEL
		AND A.FW_VERSION = :FW_VERSION
	#if ($TEST_SUITE_ID && $TEST_SUITE_ID != "" && $TEST_SUITE_ID != "null")
		AND A.TEST_SUITE_ID = :TEST_SUITE_ID
	#end
	#if ($RESULT && $RESULT != "" && $RESULT != "null")
		AND B.RESULT = :RESULT
	#end
	#if ($EXTRA2 && $EXTRA2 != "" && $EXTRA2 != "null")
		AND B.EXTRA2 = :EXTRA2
	#end
	#if ($EXTRA3 && $EXTRA3 != "" && $EXTRA3 != "null")
		AND B.EXTRA3 = :EXTRA3
	#end
	#if ($EXTRA4 && $EXTRA4 != "" && $EXTRA4 != "null")
		AND B.EXTRA4 = :EXTRA4
	#end
	#if ($EXTRA5 && $EXTRA5 != "" && $EXTRA5 != "null")
		AND B.EXTRA5 = :EXTRA5
	#end
	#if ($EXTRA6 && $EXTRA6 != "" && $EXTRA6 != "null")
		AND B.EXTRA6 = :EXTRA6
	#end
	#if ($EXTRA7 && $EXTRA7 != "" && $EXTRA7 != "null")
		AND B.EXTRA7 = :EXTRA7
	#end
	#if ($EXTRA8 && $EXTRA8 != "" && $EXTRA8 != "null")
		AND B.EXTRA8 = :EXTRA8
	#end
	#if ($EXTRA9 && $EXTRA9 != "" && $EXTRA9 != "null")
		AND B.EXTRA9 = :EXTRA9
	#end
) A
WHERE 
	1 = 1
GROUP BY
	A.TEST_CASE_ID
ORDER BY 
	MAX(A.CATEGORY), MAX(A.MODEL), MAX(A.FW_VERSION) DESC, MAX(A.TEST_CASE_ID)
				]]>
			</statement>
			<result mapper="org.anyframe.query.impl.jdbc.mapper.CallbackResultSetMapper"/>
		</query>
		<query id="ImageConfirmCycleDao.findAll" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT A.*
FROM (
	SELECT
		ROW_NUMBER() OVER(ORDER BY
			#if ($filterColumn && $filterColumn != "" && $filterColumn != "null")
				RESULT ASC ,
			#end 
				A.CYCLE
		) AS ROWNUM
		, COUNT(1) OVER() AS TOTALCOUNT
		, A.TC_IDX
		, A.CATEGORY
		, A.MODEL
		, A.FW_VERSION
		, A.CYCLE
		, A.NODE_MAC
		, A.NODE_IP
		, RESULT
		, TEST_PROJECT_ID
		, TEST_CASE_ID
		, TEST_CASE_NAME
		, SUITE_DESCRIPTION
		, ISNULL(EXTRA1, '-') AS EXTRA1
		, ISNULL(EXTRA2, '-') AS EXTRA2
		, ISNULL(EXTRA3, '-') AS EXTRA3
		, ISNULL(EXTRA4, '-') AS EXTRA4
		, ISNULL(EXTRA5, '-') AS EXTRA5
		, ISNULL(EXTRA6, '-') AS EXTRA6
		, ISNULL(EXTRA7, '-') AS EXTRA7
		, ISNULL(EXTRA8, '-') AS EXTRA8
		, ISNULL(EXTRA9, '-') AS EXTRA9
		, ISNULL(EXTRA10, '-') AS EXTRA10
		, ISNULL(EXTRA11, '-') AS EXTRA11
		, ISNULL(EXTRA12, '-') AS EXTRA12
		, ISNULL(EXTRA13, '-') AS EXTRA13
		, ISNULL(EXTRA14, '-') AS EXTRA14
		, ISNULL(EXTRA15, '-') AS EXTRA15
		, STUFF(ISNULL((SELECT ', ' + LOG_NAME FROM SQE_TC_LOG WHERE TC_IDX = A.TC_IDX GROUP BY LOG_NAME FOR XML PATH (''), TYPE).value('.','VARCHAR(MAX)'), ''), 1, 2, '') AS LOG_FILE
		, B.CAUSE
	FROM
		SQE_TEST_CASE_MASTER A LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
	WHERE 
		TEST_PROJECT_ID = 'IMAGE_CONFIRM'
		AND B.RESULT <> 'Unknown'
		AND A.TEST_SUITE_ID = :TEST_SUITE_ID
		AND A.CATEGORY = :CATEGORY
		AND A.MODEL = :MODEL
		AND A.FW_VERSION = :FW_VERSION
		AND A.TEST_CASE_ID = :TEST_CASE_ID
) A
WHERE 
	1 = 1
#if (!$loadOnce || $loadOnce == "" || $loadOnce == "null")
	AND A.ROWNUM BETWEEN (:rows * (:page - 1)) + 1 AND (:rows * :page)
#end
ORDER BY "{{sidx}}"
#if ($sord == "desc")
	DESC
#else
	ASC
#end
				]]>
			</statement>
		</query>
		<query id="ImageConfirmCycleDao.exportExcel" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT
	TEST_PROJECT_ID AS "TEST_PROJECT_ID"
	, A.CATEGORY AS "CATEGORY"
	, A.MODEL AS "MODEL"
	, A.FW_VERSION AS "FW_VERSION"
	, TEST_SUITE_ID AS "TEST_SUITE_ID"
	, TEST_CASE_NAME AS "TEST_CASE_NAME"
	, SUITE_DESCRIPTION AS "SUITE_DESCRIPTION"
	, A.NODE_MAC AS "NODE_MAC"
	, A.NODE_IP AS "NODE_IP"
#if ($CATEGORY == "NCAM")
	, ISNULL(B.EXTRA1, '-') AS "EXTRA1"
	, ISNULL(B.EXTRA2, '-') AS "EXTRA2"
	, ISNULL(B.EXTRA3, '-') AS "EXTRA3"
	, ISNULL(B.EXTRA4, '-') AS "EXTRA4"
	, ISNULL(B.EXTRA5, '-') AS "EXTRA5"
	, ISNULL(B.EXTRA6, '-') AS "EXTRA6"
	, ISNULL(B.EXTRA7, '-') AS "EXTRA7"
	, ISNULL(B.EXTRA8, '-') AS "EXTRA8"
	, ISNULL(B.EXTRA9, '-') AS "EXTRA9"
	, ISNULL(B.EXTRA10, '-') AS "EXTRA10"
	, ISNULL(B.EXTRA11, '-') AS "EXTRA11"
	, ISNULL(B.EXTRA12, '-') AS "EXTRA12"
#end
#if ($CATEGORY == "NVR")
	, ISNULL(B.EXTRA1, '-') AS "EXTRA1"
	, ISNULL(B.EXTRA3, '-') AS "EXTRA3"
	, ISNULL(B.EXTRA4, '-') AS "EXTRA4"
	, ISNULL(B.EXTRA5, '-') AS "EXTRA5"
#end
#if ($CATEGORY == "DVR")
	, ISNULL(B.EXTRA1, '-') AS "EXTRA1"
	, ISNULL(B.EXTRA2, '-') AS "EXTRA2"
	, ISNULL(B.EXTRA4, '-') AS "EXTRA4"
#end
	, ISNULL(B.EXTRA13, '-') AS "EXTRA13"
	, ISNULL(B.EXTRA14, '-') AS "EXTRA14"
	, ISNULL(B.EXTRA15, '-') AS "EXTRA15"
	, A.CYCLE AS "CYCLE"
	, RESULT AS "RESULT"
FROM
	SQE_TEST_CASE_MASTER A LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
WHERE 
	TEST_PROJECT_ID = 'IMAGE_CONFIRM'
	AND B.RESULT <> 'Unknown'
	AND A.TEST_SUITE_ID = :TEST_SUITE_ID
	AND A.CATEGORY = :CATEGORY
	AND A.MODEL = :MODEL
	AND A.FW_VERSION = :FW_VERSION
	AND A.TEST_CASE_ID = :TEST_CASE_ID
#if ($RESULT && $RESULT != "" && $RESULT != "null")
	AND B.RESULT = :RESULT
#end
ORDER BY 
	A.CYCLE
				]]>
			</statement>
			<result mapper="org.anyframe.query.impl.jdbc.mapper.CallbackResultSetMapper"/>
		</query>
		<query id="ImageConfirmDetailDao.findAll" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT
	C.SEQ
	, C.IMAGE_PATH
	, C.RGB
	, C.VERTICAL_FLIP
	, C.HORIZONTAL_FLIP
	, CONVERT(VARCHAR(19), B.START_TIME, 120) AS START_TIME
	, CONVERT(VARCHAR(19), B.END_TIME, 120) AS END_TIME
	, CONVERT(VARCHAR(19), B.INSERT_DATE, 120) AS INSERT_DATE
	, CONVERT(VARCHAR(10), DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)/86400000) + 'Days ' +
	CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%86400000)/3600000) + 'h '+
	CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%3600000)/60000) + 'm '+
	CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%60000)/1000) + 's' AS EXECUTION_TIME
FROM
	SQE_TEST_CASE_MASTER A 
	LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
	LEFT OUTER JOIN SQE_IMAGE_CONFIRM_RESULT C ON A.TC_IDX = C.TC_IDX
WHERE 
	A.TC_IDX = :TC_IDX
				]]>
			</statement>
		</query>
		<query id="ImageConfirmDetailDao.exportExcel" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT
	TEST_PROJECT_ID AS "TEST_PROJECT_ID"
	, A.CATEGORY AS "CATEGORY"
	, A.MODEL AS "MODEL"
	, A.FW_VERSION AS "FW_VERSION"
	, TEST_SUITE_ID AS "TEST_SUITE_ID"
	, TEST_CASE_NAME AS "TEST_CASE_NAME"
	, SUITE_DESCRIPTION AS "SUITE_DESCRIPTION"
	, A.NODE_MAC AS "NODE_MAC"
	, A.NODE_IP AS "NODE_IP"
#if ($CATEGORY == "NCAM")
	, ISNULL(B.EXTRA1, '-') AS "EXTRA1"
	, ISNULL(B.EXTRA2, '-') AS "EXTRA2"
	, ISNULL(B.EXTRA3, '-') AS "EXTRA3"
	, ISNULL(B.EXTRA4, '-') AS "EXTRA4"
	, ISNULL(B.EXTRA5, '-') AS "EXTRA5"
	, ISNULL(B.EXTRA6, '-') AS "EXTRA6"
	, ISNULL(B.EXTRA7, '-') AS "EXTRA7"
	, ISNULL(B.EXTRA8, '-') AS "EXTRA8"
	, ISNULL(B.EXTRA9, '-') AS "EXTRA9"
	, ISNULL(B.EXTRA10, '-') AS "EXTRA10"
	, ISNULL(B.EXTRA11, '-') AS "EXTRA11"
	, ISNULL(B.EXTRA12, '-') AS "EXTRA12"
#end
#if ($CATEGORY == "NVR")
	, ISNULL(B.EXTRA1, '-') AS "EXTRA1"
	, ISNULL(B.EXTRA3, '-') AS "EXTRA3"
	, ISNULL(B.EXTRA4, '-') AS "EXTRA4"
	, ISNULL(B.EXTRA5, '-') AS "EXTRA5"
#end
#if ($CATEGORY == "DVR")
	, ISNULL(B.EXTRA1, '-') AS "EXTRA1"
	, ISNULL(B.EXTRA2, '-') AS "EXTRA2"
	, ISNULL(B.EXTRA4, '-') AS "EXTRA4"
#end
	, ISNULL(B.EXTRA13, '-') AS "EXTRA13"
	, ISNULL(B.EXTRA14, '-') AS "EXTRA14"
	, ISNULL(B.EXTRA15, '-') AS "EXTRA15"
	, A.CYCLE AS "Cycle"
	, RESULT AS "Result"
	, C.RGB AS "RGB Result"
	, C.VERTICAL_FLIP AS "Flip Vertically Result"
	, C.HORIZONTAL_FLIP AS "Flip Horizontally Result"
	, B.CAUSE AS "Cause"
	, CONVERT(VARCHAR(19), B.START_TIME, 120) AS "Start Time"
	, CONVERT(VARCHAR(19), B.END_TIME, 120) AS "End Time"
	, CONVERT(VARCHAR(19), B.INSERT_DATE, 120) AS "Insert Date"
	, CONVERT(VARCHAR(10), DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)/86400000) + 'Days ' +
	CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%86400000)/3600000) + 'h '+
	CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%3600000)/60000) + 'm '+
	CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%60000)/1000) + 's' AS "Execution Time"
FROM
	SQE_TEST_CASE_MASTER A 
	LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
	LEFT OUTER JOIN SQE_IMAGE_CONFIRM_RESULT C ON A.TC_IDX = C.TC_IDX
WHERE 
	A.TC_IDX = :TC_IDX
ORDER BY 
	A.TC_IDX
				]]>
			</statement>
			<result mapper="org.anyframe.query.impl.jdbc.mapper.CallbackResultSetMapper"/>
		</query>
	</queries>
</queryservice>