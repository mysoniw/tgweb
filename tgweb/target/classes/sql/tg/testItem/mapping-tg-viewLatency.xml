<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE queryservice PUBLIC "-//ANYFRAME//DTD QUERYSERVICE//EN" 
	"http://www.anyframejava.org/dtd/anyframe-core-query-mapping-3.2.dtd">
<queryservice>
	<queries>
		<query id="ViewLatencyMainDao.findAll" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT A.*
FROM (
	SELECT
		ROW_NUMBER() OVER(ORDER BY MAX(A.CATEGORY), MAX(A.MODEL), MAX(A.FW_VERSION) DESC) AS ROWNUM
		, COUNT(1) OVER() AS TOTALCOUNT
		, MAX(TC_IDX) AS TC_IDX
		, MAX(A.CATEGORY) AS CATEGORY
		, MAX(A.MODEL) AS MODEL
		, MAX(A.FW_VERSION) AS FW_VERSION
		, MAX(A.TEST_PROJECT_ID) AS TEST_PROJECT_ID
		, MAX(A.TEST_SUITE_ID) AS TEST_SUITE_ID
		, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
		, COUNT(TOTAL_CYCLE) AS TEST_CASE_COUNT
		, SUM(A.NG_INT) AS NG
		, MAX(EXTRA3) AS EXTRA3
	FROM
		(
		SELECT
			MAX(TC_IDX) AS TC_IDX
			, MAX(A.CATEGORY) AS CATEGORY
			, MAX(A.MODEL) AS MODEL
			, MAX(A.FW_VERSION) AS FW_VERSION
			, MAX(A.TEST_PROJECT_ID) AS TEST_PROJECT_ID
			, MAX(A.TEST_SUITE_ID) AS TEST_SUITE_ID
			, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
			, CASE 
				WHEN SUM(A.NG_INT) > 0 THEN 1
				ELSE 0
			END AS NG_INT
			, COUNT(A.TEST_CASE_ID) AS TOTAL_CYCLE
			, MAX(EXTRA3) AS EXTRA3
		FROM
			(
			SELECT
				A.TC_IDX
				, A.CATEGORY
				, A.MODEL
				, A.FW_VERSION
				, CASE RESULT 
					WHEN 'NG' THEN 1
					ELSE 0
				END AS NG_INT
				, TEST_PROJECT_ID
				, TEST_SUITE_ID
				, TEST_CASE_ID
				, TEST_CASE_NAME
				, SUITE_DESCRIPTION
				, EXTRA3
			FROM
				SQE_TEST_CASE_MASTER A LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
			WHERE 
				TEST_PROJECT_ID = 'VIEW_LATENCY'
				AND B.RESULT <> 'Unknown'
			#if ($CATEGORY && $CATEGORY != "" && $CATEGORY != "null")
				AND A.CATEGORY = :CATEGORY
			#end
			#if ($MODEL && $MODEL != "" && $MODEL != "null")
				AND A.MODEL = :MODEL
			#end
			#if ($FW_VERSION && $FW_VERSION != "" && $FW_VERSION != "null")
				AND A.FW_VERSION = :FW_VERSION
			#end
			#if ($TEST_SUITE_ID && $TEST_SUITE_ID != "" && $TEST_SUITE_ID != "null")
				AND A.TEST_SUITE_ID = :TEST_SUITE_ID
			#end
		) A
		WHERE 
			1 = 1
		GROUP BY
			A.CATEGORY, A.MODEL, A.FW_VERSION, A.TEST_SUITE_ID, A.TEST_CASE_ID
	) A
	WHERE
		1 = 1
	GROUP BY
		A.CATEGORY, A.MODEL, A.FW_VERSION, A.TEST_SUITE_ID
) A
WHERE 
	1 = 1
#if (!$loadOnce || $loadOnce == "" || $loadOnce == "null")
	AND A.ROWNUM BETWEEN (:rows * (:page - 1)) + 1 AND (:rows * :page)
#end
ORDER BY "{{sidx}}"
#if ($sord == "desc")
	DESC
#else
	ASC
#end
				]]>
			</statement>
		</query>
		<query id="ViewLatencyMainDao.exportExcel" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT
	MAX(A.TEST_PROJECT_ID) AS "TEST_PROJECT_ID"
	, MAX(A.CATEGORY) AS "CATEGORY"
	, MAX(A.MODEL) AS "MODEL"
	, MAX(A.FW_VERSION) AS "FW_VERSION"
	, MAX(A.TEST_SUITE_ID) AS "TEST_SUITE_ID"
	, MAX(A.SUITE_DESCRIPTION) AS "SUITE_DESCRIPTION"
	, COUNT(TOTAL_CYCLE) AS "TOTAL_TC"
	, SUM(A.NG_INT) AS "NG"
FROM
	(
	SELECT
		MAX(TC_IDX) AS TC_IDX
		, MAX(A.CATEGORY) AS CATEGORY
		, MAX(A.MODEL) AS MODEL
		, MAX(A.FW_VERSION) AS FW_VERSION
		, MAX(A.TEST_PROJECT_ID) AS TEST_PROJECT_ID
		, MAX(A.TEST_SUITE_ID) AS TEST_SUITE_ID
		, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
		, CASE 
			WHEN SUM(A.NG_INT) > 0 THEN 1
			ELSE 0
		END AS NG_INT
		, COUNT(A.TEST_CASE_ID) AS TOTAL_CYCLE
	FROM
		(
		SELECT
			A.TC_IDX
			, A.CATEGORY
			, A.MODEL
			, A.FW_VERSION
			, CASE RESULT 
				WHEN 'NG' THEN 1
				ELSE 0
			END AS NG_INT
			, TEST_PROJECT_ID
			, TEST_SUITE_ID
			, TEST_CASE_ID
			, TEST_CASE_NAME
			, SUITE_DESCRIPTION
		FROM
			SQE_TEST_CASE_MASTER A LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
		WHERE 
			TEST_PROJECT_ID = 'VIEW_LATENCY'
			AND B.RESULT <> 'Unknown'
		#if ($CATEGORY && $CATEGORY != "" && $CATEGORY != "null")
			AND A.CATEGORY = :CATEGORY
		#end
		#if ($MODEL && $MODEL != "" && $MODEL != "null")
			AND A.MODEL = :MODEL
		#end
		#if ($FW_VERSION && $FW_VERSION != "" && $FW_VERSION != "null")
			AND A.FW_VERSION = :FW_VERSION
		#end
		#if ($TEST_SUITE_ID && $TEST_SUITE_ID != "" && $TEST_SUITE_ID != "null")
			AND A.TEST_SUITE_ID = :TEST_SUITE_ID
		#end
	) A
	WHERE 
		1 = 1
	GROUP BY
		A.CATEGORY, A.MODEL, A.FW_VERSION, A.TEST_SUITE_ID, A.TEST_CASE_ID
) A
WHERE
	1 = 1
GROUP BY
	A.CATEGORY, A.MODEL, A.FW_VERSION, A.TEST_SUITE_ID
ORDER BY
	MAX(A.CATEGORY), MAX(A.MODEL), MAX(A.FW_VERSION) DESC
				]]>
			</statement>
		</query>
		<query id="ViewLatencyTcDao.findAll" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT A.*
FROM (
	SELECT
		ROW_NUMBER() OVER(ORDER BY MAX(A.CATEGORY), MAX(A.MODEL), MAX(A.FW_VERSION) DESC, MAX(A.TEST_CASE_ID)) AS ROWNUM
		, COUNT(1) OVER() AS TOTALCOUNT
		, MAX(A.TC_IDX) AS TC_IDX
		, MAX(A.CATEGORY) AS CATEGORY
		, MAX(A.MODEL) AS MODEL
		, MAX(A.FW_VERSION) AS FW_VERSION
		, MAX(A.TEST_PROJECT_ID) AS TEST_PROJECT_ID
		, MAX(A.TEST_SUITE_ID) AS TEST_SUITE_ID
		, A.TEST_CASE_ID
		, MAX(A.TEST_CASE_NAME) AS TEST_CASE_NAME
		, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
		, MAX(NODE_MAC) AS NODE_MAC
		, MAX(NODE_IP) AS NODE_IP
		, ISNULL(MAX(A.EXTRA1), '-') AS EXTRA1
		, ISNULL(MAX(A.EXTRA2), '-') AS EXTRA2
		, ISNULL(MAX(A.EXTRA3), '-') AS EXTRA3
		, ISNULL(MAX(A.EXTRA4), '-') AS EXTRA4
		, ISNULL(MAX(A.EXTRA5), '-') AS EXTRA5
		, ISNULL(MAX(A.EXTRA6), '-') AS EXTRA6
		, ISNULL(MAX(A.EXTRA7), '-') AS EXTRA7
		, ISNULL(MAX(A.EXTRA8), '-') AS EXTRA8
		, ISNULL(MAX(A.EXTRA9), '-') AS EXTRA9
		, ISNULL(MAX(A.EXTRA10), '-') AS EXTRA10
		, ISNULL(MAX(A.EXTRA11), '-') AS EXTRA11
		, ISNULL(MAX(A.EXTRA12), '-') AS EXTRA12
		, ISNULL(MAX(A.EXTRA13), '-') AS EXTRA13
		, ISNULL(MAX(A.EXTRA14), '-') AS EXTRA14
		, ISNULL(MAX(A.EXTRA15), '-') AS EXTRA15
		, COUNT(DISTINCT A.TC_IDX) AS TOTAL_CYCLE
		, SUM(A.NG_INT) AS NG
		, CAST(MAX(C.TIME) AS VARCHAR) AS MAX_TIME
		, CAST(MIN(C.TIME) AS VARCHAR) AS MIN_TIME
		, CAST(ROUND(AVG(C.TIME), 2) AS VARCHAR) AS AVG_TIME
		, CAST(STDEV(C.TIME) AS VARCHAR) AS STDEV_TIME
		, CAST(MAX(C.DEVIATION) AS VARCHAR) AS MAX_DEVIATION
		, CAST(MIN(C.DEVIATION) AS VARCHAR) AS MIN_DEVIATION
		, CAST(ROUND(AVG(C.DEVIATION), 2) AS VARCHAR) AS AVG_DEVIATION
		, CAST(STDEV(C.DEVIATION) AS VARCHAR) AS STDEV_DEVIATION
	FROM
		(
		SELECT
			MAX(A.TC_IDX) AS TC_IDX
			, MAX(A.CATEGORY) AS CATEGORY
			, MAX(A.MODEL) AS MODEL
			, MAX(A.FW_VERSION) AS FW_VERSION
			, CASE MAX(RESULT)
				WHEN 'NG' THEN 1
				ELSE 0
			END AS NG_INT
			, MAX(TEST_PROJECT_ID) AS TEST_PROJECT_ID
			, MAX(TEST_SUITE_ID) AS TEST_SUITE_ID
			, MAX(TEST_CASE_ID) AS TEST_CASE_ID
			, MAX(TEST_CASE_NAME) AS TEST_CASE_NAME
			, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
			, MAX(NODE_MAC) AS NODE_MAC
			, MAX(NODE_IP) AS NODE_IP
			, MAX(EXTRA1) AS EXTRA1
			, MAX(EXTRA2) AS EXTRA2
			, MAX(EXTRA3) AS EXTRA3
			, MAX(EXTRA4) AS EXTRA4
			, MAX(EXTRA5) AS EXTRA5
			, MAX(EXTRA6) AS EXTRA6
			, MAX(EXTRA7) AS EXTRA7
			, MAX(EXTRA8) AS EXTRA8
			, MAX(EXTRA9) AS EXTRA9
			, MAX(EXTRA10) AS EXTRA10
			, MAX(EXTRA11) AS EXTRA11
			, MAX(EXTRA12) AS EXTRA12
			, MAX(EXTRA13) AS EXTRA13
			, MAX(EXTRA14) AS EXTRA14
			, MAX(EXTRA15) AS EXTRA15
		FROM
			SQE_TEST_CASE_MASTER A 
			LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
		WHERE 
			TEST_PROJECT_ID = 'VIEW_LATENCY'
			AND B.RESULT <> 'Unknown'
			AND A.CATEGORY = :CATEGORY
			AND A.MODEL = :MODEL
			AND A.FW_VERSION = :FW_VERSION
			AND A.TEST_SUITE_ID = :TEST_SUITE_ID
		#if ($EXTRA2 && $EXTRA2 != "" && $EXTRA2 != "null")
			AND B.EXTRA2 = :EXTRA2
		#end
		#if ($EXTRA3 && $EXTRA3 != "" && $EXTRA3 != "null")
			AND B.EXTRA3 = :EXTRA3
		#end
		#if ($EXTRA4 && $EXTRA4 != "" && $EXTRA4 != "null")
			AND B.EXTRA4 = :EXTRA4
		#end
		#if ($EXTRA5 && $EXTRA5 != "" && $EXTRA5 != "null")
			AND B.EXTRA5 = :EXTRA5
		#end
		#if ($EXTRA6 && $EXTRA6 != "" && $EXTRA6 != "null")
			AND B.EXTRA6 = :EXTRA6
		#end
		#if ($EXTRA7 && $EXTRA7 != "" && $EXTRA7 != "null")
			AND B.EXTRA7 = :EXTRA7
		#end
		#if ($EXTRA8 && $EXTRA8 != "" && $EXTRA8 != "null")
			AND B.EXTRA8 = :EXTRA8
		#end
		#if ($EXTRA9 && $EXTRA9 != "" && $EXTRA9 != "null")
			AND B.EXTRA9 = :EXTRA9
		#end
		GROUP BY 
			A.TC_IDX
	) A
	LEFT OUTER JOIN SQE_VIEW_LATENCY_RESULT C ON A.TC_IDX = C.TC_IDX
	WHERE 
		1 = 1
	GROUP BY
		A.TEST_CASE_ID
) A
WHERE 
	1 = 1
#if (!$loadOnce || $loadOnce == "" || $loadOnce == "null")
	AND A.ROWNUM BETWEEN (:rows * (:page - 1)) + 1 AND (:rows * :page)
#end
ORDER BY "{{sidx}}"
#if ($sord == "desc")
	DESC
#else
	ASC
#end
				]]>
			</statement>
		</query>
		<query id="ViewLatencyTcDao.exportExcel" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT
	MAX(A.TEST_PROJECT_ID) AS "TEST_PROJECT_ID"
	, MAX(A.CATEGORY) AS "CATEGORY"
	, MAX(A.MODEL) AS "MODEL"
	, MAX(A.FW_VERSION) AS "FW_VERSION"
	, MAX(A.TEST_SUITE_ID) AS "TEST_SUITE_ID"
	, MAX(A.TEST_CASE_NAME) AS "TEST_CASE_NAME"
	, MAX(A.SUITE_DESCRIPTION) AS "SUITE_DESCRIPTION"
#if ($CATEGORY == "NCAM")
	, ISNULL(MAX(A.EXTRA1), '-') AS "EXTRA1"
	, ISNULL(MAX(A.EXTRA2), '-') AS "EXTRA2"
	, ISNULL(MAX(A.EXTRA3), '-') AS "EXTRA3"
	, ISNULL(MAX(A.EXTRA4), '-') AS "EXTRA4"
	, ISNULL(MAX(A.EXTRA5), '-') AS "EXTRA5"
	, ISNULL(MAX(A.EXTRA6), '-') AS "EXTRA6"
	, ISNULL(MAX(A.EXTRA7), '-') AS "EXTRA7"
	, ISNULL(MAX(A.EXTRA8), '-') AS "EXTRA8"
	, ISNULL(MAX(A.EXTRA9), '-') AS "EXTRA9"
	, ISNULL(MAX(A.EXTRA10), '-') AS "EXTRA10"
	, ISNULL(MAX(A.EXTRA11), '-') AS "EXTRA11"
	, ISNULL(MAX(A.EXTRA12), '-') AS "EXTRA12"
#end
#if ($CATEGORY == "NVR")
	, ISNULL(MAX(A.EXTRA1), '-') AS "EXTRA1"
	, ISNULL(MAX(A.EXTRA3), '-') AS "EXTRA3"
	, ISNULL(MAX(A.EXTRA4), '-') AS "EXTRA4"
	, ISNULL(MAX(A.EXTRA5), '-') AS "EXTRA5"
#end
#if ($CATEGORY == "DVR")
	, ISNULL(MAX(A.EXTRA1), '-') AS "EXTRA1"
	, ISNULL(MAX(A.EXTRA2), '-') AS "EXTRA2"
	, ISNULL(MAX(A.EXTRA4), '-') AS "EXTRA4"
#end
	, CAST(MAX(C.TIME) AS VARCHAR) AS "Max Delay(s)"
	, CAST(MIN(C.TIME) AS VARCHAR) AS "Min Delay(s)"
	, CAST(ROUND(AVG(C.TIME), 2) AS VARCHAR) AS "Avg Delay(s)"
	, CAST(STDEV(C.TIME) AS VARCHAR) AS "Standard Deviation Delay(s)"
	, CAST(MAX(C.DEVIATION) AS VARCHAR) AS "Max Deviation(s)"
	, CAST(MIN(C.DEVIATION) AS VARCHAR) AS "Min Deviation(s)"
	, CAST(ROUND(AVG(C.DEVIATION), 2) AS VARCHAR) AS "Avg Deviation(s)"
	, CAST(STDEV(C.DEVIATION) AS VARCHAR) AS "Standard Deviation Deviation(s)"
	, COUNT(DISTINCT A.TC_IDX) AS "TOTAL_CYCLE"
	, SUM(A.NG_INT) AS "NG"
FROM
(
	SELECT
		MAX(A.TC_IDX) AS TC_IDX
		, MAX(A.CATEGORY) AS CATEGORY
		, MAX(A.MODEL) AS MODEL
		, MAX(A.FW_VERSION) AS FW_VERSION
		, CASE MAX(RESULT)
			WHEN 'NG' THEN 1
			ELSE 0
		END AS NG_INT
		, MAX(TEST_PROJECT_ID) AS TEST_PROJECT_ID
		, MAX(TEST_SUITE_ID) AS TEST_SUITE_ID
		, MAX(TEST_CASE_ID) AS TEST_CASE_ID
		, MAX(TEST_CASE_NAME) AS TEST_CASE_NAME
		, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
		, MAX(NODE_MAC) AS NODE_MAC
		, MAX(NODE_IP) AS NODE_IP
		, MAX(EXTRA1) AS EXTRA1
		, MAX(EXTRA2) AS EXTRA2
		, MAX(EXTRA3) AS EXTRA3
		, MAX(EXTRA4) AS EXTRA4
		, MAX(EXTRA5) AS EXTRA5
		, MAX(EXTRA6) AS EXTRA6
		, MAX(EXTRA7) AS EXTRA7
		, MAX(EXTRA8) AS EXTRA8
		, MAX(EXTRA9) AS EXTRA9
		, MAX(EXTRA10) AS EXTRA10
		, MAX(EXTRA11) AS EXTRA11
		, MAX(EXTRA12) AS EXTRA12
		, MAX(EXTRA13) AS EXTRA13
		, MAX(EXTRA14) AS EXTRA14
		, MAX(EXTRA15) AS EXTRA15
	FROM
		SQE_TEST_CASE_MASTER A 
		LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
	WHERE 
		TEST_PROJECT_ID = 'VIEW_LATENCY'
		AND B.RESULT <> 'Unknown'
		AND A.CATEGORY = :CATEGORY
		AND A.MODEL = :MODEL
		AND A.FW_VERSION = :FW_VERSION
	#if ($TEST_SUITE_ID && $TEST_SUITE_ID != "" && $TEST_SUITE_ID != "null")
		AND A.TEST_SUITE_ID = :TEST_SUITE_ID
	#end
	#if ($EXTRA2 && $EXTRA2 != "" && $EXTRA2 != "null")
		AND B.EXTRA2 = :EXTRA2
	#end
	#if ($EXTRA3 && $EXTRA3 != "" && $EXTRA3 != "null")
		AND B.EXTRA3 = :EXTRA3
	#end
	#if ($EXTRA4 && $EXTRA4 != "" && $EXTRA4 != "null")
		AND B.EXTRA4 = :EXTRA4
	#end
	#if ($EXTRA5 && $EXTRA5 != "" && $EXTRA5 != "null")
		AND B.EXTRA5 = :EXTRA5
	#end
	#if ($EXTRA6 && $EXTRA6 != "" && $EXTRA6 != "null")
		AND B.EXTRA6 = :EXTRA6
	#end
	#if ($EXTRA7 && $EXTRA7 != "" && $EXTRA7 != "null")
		AND B.EXTRA7 = :EXTRA7
	#end
	#if ($EXTRA8 && $EXTRA8 != "" && $EXTRA8 != "null")
		AND B.EXTRA8 = :EXTRA8
	#end
	#if ($EXTRA9 && $EXTRA9 != "" && $EXTRA9 != "null")
		AND B.EXTRA9 = :EXTRA9
	#end
	GROUP BY 
		A.TC_IDX
) A
LEFT OUTER JOIN SQE_VIEW_LATENCY_RESULT C ON A.TC_IDX = C.TC_IDX
WHERE 
	1 = 1
GROUP BY
	A.TEST_CASE_ID
ORDER BY 
	MAX(A.CATEGORY), MAX(A.MODEL), MAX(A.FW_VERSION) DESC, MAX(A.TEST_CASE_ID)
				]]>
			</statement>
			<result mapper="org.anyframe.query.impl.jdbc.mapper.CallbackResultSetMapper"/>
		</query>
		<query id="ViewLatencyCycleDao.findAll" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT A.*
FROM (
	SELECT
		ROW_NUMBER() OVER(ORDER BY MAX(A.CYCLE)) AS ROWNUM
		, COUNT(1) OVER() AS TOTALCOUNT
		, A.TC_IDX
		, MAX(A.CATEGORY) AS CATEGORY
		, MAX(A.MODEL) AS MODEL
		, MAX(A.FW_VERSION) AS FW_VERSION
		, MAX(A.CYCLE) AS CYCLE
		, MAX(A.NODE_MAC) AS NODE_MAC
		, MAX(A.NODE_IP) AS NODE_IP
		, CASE WHEN MAX(RESULT) = 'NG' THEN 'NG' ELSE '-' END AS RESULT
		, MAX(TEST_PROJECT_ID) AS TEST_PROJECT_ID
		, MAX(TEST_CASE_ID) AS TEST_CASE_ID
		, MAX(TEST_CASE_NAME) AS TEST_CASE_NAME
		, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
		, ISNULL(MAX(EXTRA1), '-') AS EXTRA1
		, ISNULL(MAX(EXTRA2), '-') AS EXTRA2
		, ISNULL(MAX(EXTRA3), '-') AS EXTRA3
		, ISNULL(MAX(EXTRA4), '-') AS EXTRA4
		, ISNULL(MAX(EXTRA5), '-') AS EXTRA5
		, ISNULL(MAX(EXTRA6), '-') AS EXTRA6
		, ISNULL(MAX(EXTRA7), '-') AS EXTRA7
		, ISNULL(MAX(EXTRA8), '-') AS EXTRA8
		, ISNULL(MAX(EXTRA9), '-') AS EXTRA9
		, ISNULL(MAX(EXTRA10), '-') AS EXTRA10
		, ISNULL(MAX(EXTRA11), '-') AS EXTRA11
		, ISNULL(MAX(EXTRA12), '-') AS EXTRA12
		, ISNULL(MAX(EXTRA13), '-') AS EXTRA13
		, ISNULL(MAX(EXTRA14), '-') AS EXTRA14
		, ISNULL(MAX(EXTRA15), '-') AS EXTRA15
		, CAST(MAX(C.TIME) AS VARCHAR) AS MAX_TIME
		, CAST(MIN(C.TIME) AS VARCHAR) AS MIN_TIME
		, CAST(ROUND(AVG(C.TIME), 2) AS VARCHAR) AS AVG_TIME
		, CAST(STDEV(C.TIME) AS VARCHAR) AS STDEV_TIME
		, CAST(MAX(C.DEVIATION) AS VARCHAR) AS MAX_DEVIATION
		, CAST(MIN(C.DEVIATION) AS VARCHAR) AS MIN_DEVIATION
		, CAST(ROUND(AVG(C.DEVIATION), 2) AS VARCHAR) AS AVG_DEVIATION
		, CAST(STDEV(C.DEVIATION) AS VARCHAR) AS STDEV_DEVIATION
		, CONVERT(VARCHAR(10), DATEDIFF(MILLISECOND, MAX(B.START_TIME), MAX(B.END_TIME))/86400000) + 'Days ' +
		CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, MAX(B.START_TIME), MAX(B.END_TIME))%86400000)/3600000) + 'h '+
		CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, MAX(B.START_TIME), MAX(B.END_TIME))%3600000)/60000) + 'm '+
		CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, MAX(B.START_TIME), MAX(B.END_TIME))%60000)/1000) + 's' AS EXECUTION_TIME
		, STUFF(ISNULL((SELECT ', ' + LOG_NAME FROM SQE_TC_LOG WHERE TC_IDX = A.TC_IDX GROUP BY LOG_NAME FOR XML PATH (''), TYPE).value('.','VARCHAR(MAX)'), ''), 1, 2, '') AS LOG_FILE
	FROM
		SQE_TEST_CASE_MASTER A 
		LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
		LEFT OUTER JOIN SQE_VIEW_LATENCY_RESULT C ON B.TC_IDX = C.TC_IDX
	WHERE 
		TEST_PROJECT_ID = 'VIEW_LATENCY'
		AND B.RESULT <> 'Unknown'
		AND A.CATEGORY = :CATEGORY
		AND A.MODEL = :MODEL
		AND A.FW_VERSION = :FW_VERSION
		AND A.TEST_CASE_ID = :TEST_CASE_ID
	GROUP BY
		A.TC_IDX
) A
WHERE 
	1 = 1
#if (!$loadOnce || $loadOnce == "" || $loadOnce == "null")
	AND A.ROWNUM BETWEEN (:rows * (:page - 1)) + 1 AND (:rows * :page)
#end
ORDER BY "{{sidx}}"
#if ($sord == "desc")
	DESC
#else
	ASC
#end
				]]>
			</statement>
		</query>
		<query id="ViewLatencyCycleDao.exportExcel" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT
	MAX(TEST_PROJECT_ID) AS "TEST_PROJECT_ID"
	, MAX(A.CATEGORY) AS "CATEGORY"
	, MAX(A.MODEL) AS "MODEL"
	, MAX(A.FW_VERSION) AS "FW_VERSION"
	, MAX(TEST_SUITE_ID) AS "TEST_SUITE_ID"
	, MAX(TEST_CASE_NAME) AS "TEST_CASE_NAME"
	, MAX(A.SUITE_DESCRIPTION) AS "SUITE_DESCRIPTION"
	, MAX(A.NODE_MAC) AS "NODE_MAC"
	, MAX(A.NODE_IP) AS "NODE_IP"
#if ($CATEGORY == "NCAM")
	, ISNULL(MAX(B.EXTRA1), '-') AS "EXTRA1"
	, ISNULL(MAX(B.EXTRA2), '-') AS "EXTRA2"
	, ISNULL(MAX(B.EXTRA3), '-') AS "EXTRA3"
	, ISNULL(MAX(B.EXTRA4), '-') AS "EXTRA4"
	, ISNULL(MAX(B.EXTRA5), '-') AS "EXTRA5"
	, ISNULL(MAX(B.EXTRA6), '-') AS "EXTRA6"
	, ISNULL(MAX(B.EXTRA7), '-') AS "EXTRA7"
	, ISNULL(MAX(B.EXTRA8), '-') AS "EXTRA8"
	, ISNULL(MAX(B.EXTRA9), '-') AS "EXTRA9"
	, ISNULL(MAX(B.EXTRA10), '-') AS "EXTRA10"
	, ISNULL(MAX(B.EXTRA11), '-') AS "EXTRA11"
	, ISNULL(MAX(B.EXTRA12), '-') AS "EXTRA12"
#end
#if ($CATEGORY == "NVR")
	, ISNULL(MAX(B.EXTRA1), '-') AS "EXTRA1"
	, ISNULL(MAX(B.EXTRA3), '-') AS "EXTRA3"
	, ISNULL(MAX(B.EXTRA4), '-') AS "EXTRA4"
	, ISNULL(MAX(B.EXTRA5), '-') AS "EXTRA5"
#end
#if ($CATEGORY == "DVR")
	, ISNULL(MAX(B.EXTRA1), '-') AS "EXTRA1"
	, ISNULL(MAX(B.EXTRA2), '-') AS "EXTRA2"
	, ISNULL(MAX(B.EXTRA4), '-') AS "EXTRA4"
#end
	, MAX(A.CYCLE) AS "CYCLE"
	, CASE WHEN MAX(RESULT) = 'NG' THEN 'NG' ELSE '-' END AS "NG"
	, CAST(MAX(C.TIME) AS VARCHAR) AS "Max Delay(s)"
	, CAST(MIN(C.TIME) AS VARCHAR) AS "Min Delay(s)"
	, CAST(ROUND(AVG(C.TIME), 2) AS VARCHAR) AS "Avg Delay(s)"
	, CAST(STDEV(C.TIME) AS VARCHAR) AS "Standard Deviation Delay(s)"
	, CAST(MAX(C.DEVIATION) AS VARCHAR) AS "Max Deviation(s)"
	, CAST(MIN(C.DEVIATION) AS VARCHAR) AS "Min Deviation(s)"
	, CAST(ROUND(AVG(C.DEVIATION), 2) AS VARCHAR) AS "Avg Deviation(s)"
	, CAST(STDEV(C.DEVIATION) AS VARCHAR) AS "Standard Deviation Deviation(s)"
FROM
	SQE_TEST_CASE_MASTER A 
	LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
	LEFT OUTER JOIN SQE_VIEW_LATENCY_RESULT C ON B.TC_IDX = C.TC_IDX
WHERE 
	TEST_PROJECT_ID = 'VIEW_LATENCY'
	AND B.RESULT <> 'Unknown'
	AND A.CATEGORY = :CATEGORY
	AND A.MODEL = :MODEL
	AND A.FW_VERSION = :FW_VERSION
	AND A.TEST_CASE_ID = :TEST_CASE_ID
GROUP BY
	A.TC_IDX
ORDER BY 
	MAX(A.CYCLE)
				]]>
			</statement>
			<result mapper="org.anyframe.query.impl.jdbc.mapper.CallbackResultSetMapper"/>
		</query>
		<query id="ViewLatencyDetailDao.findAll" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT A.*
FROM (
	SELECT
		ROW_NUMBER() OVER(ORDER BY C.SEQ) AS ROWNUM
		, COUNT(1) OVER() AS TOTALCOUNT
		, C.SEQ
		, C.COLOR
		, C.TIME
		, C.PERCENTAGE
		, C.CNT
		, C.DEVIATION
		, CONVERT(VARCHAR(19), B.START_TIME, 120) AS START_TIME
		, CONVERT(VARCHAR(19), B.END_TIME, 120) AS END_TIME
		, CONVERT(VARCHAR(19), B.INSERT_DATE, 120) AS INSERT_DATE
		, CONVERT(VARCHAR(10), DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)/86400000) + 'Days ' +
		CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%86400000)/3600000) + 'h '+
		CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%3600000)/60000) + 'm '+
		CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%60000)/1000) + 's' AS EXECUTION_TIME
	FROM
		SQE_TEST_CASE_MASTER A 
		LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
		LEFT OUTER JOIN SQE_VIEW_LATENCY_RESULT C ON A.TC_IDX = C.TC_IDX
	WHERE 
		A.TC_IDX = :TC_IDX
) A
WHERE 
	1 = 1
#if (!$loadOnce || $loadOnce == "" || $loadOnce == "null")
	AND A.ROWNUM BETWEEN (:rows * (:page - 1)) + 1 AND (:rows * :page)
#end
ORDER BY "{{sidx}}"
#if ($sord == "desc")
	DESC
#else
	ASC
#end
				]]>
			</statement>
		</query>
		<query id="ViewLatencyDetailDao.exportExcel" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT
	TEST_PROJECT_ID AS "TEST_PROJECT_ID"
	, A.CATEGORY AS "CATEGORY"
	, A.MODEL AS "MODEL"
	, A.FW_VERSION AS "FW_VERSION"
	, TEST_SUITE_ID AS "TEST_SUITE_ID"
	, TEST_CASE_NAME AS "TEST_CASE_NAME"
	, SUITE_DESCRIPTION AS "SUITE_DESCRIPTION"
	, A.NODE_MAC AS "NODE_MAC"
	, A.NODE_IP AS "NODE_IP"
#if ($CATEGORY == "NCAM")
	, ISNULL(B.EXTRA1, '-') AS "EXTRA1"
	, ISNULL(B.EXTRA2, '-') AS "EXTRA2"
	, ISNULL(B.EXTRA3, '-') AS "EXTRA3"
	, ISNULL(B.EXTRA4, '-') AS "EXTRA4"
	, ISNULL(B.EXTRA5, '-') AS "EXTRA5"
	, ISNULL(B.EXTRA6, '-') AS "EXTRA6"
	, ISNULL(B.EXTRA7, '-') AS "EXTRA7"
	, ISNULL(B.EXTRA8, '-') AS "EXTRA8"
	, ISNULL(B.EXTRA9, '-') AS "EXTRA9"
	, ISNULL(B.EXTRA10, '-') AS "EXTRA10"
	, ISNULL(B.EXTRA11, '-') AS "EXTRA11"
	, ISNULL(B.EXTRA12, '-') AS "EXTRA12"
#end
#if ($CATEGORY == "NVR")
	, ISNULL(B.EXTRA1, '-') AS "EXTRA1"
	, ISNULL(B.EXTRA3, '-') AS "EXTRA3"
	, ISNULL(B.EXTRA4, '-') AS "EXTRA4"
	, ISNULL(B.EXTRA5, '-') AS "EXTRA5"
#end
#if ($CATEGORY == "DVR")
	, ISNULL(B.EXTRA1, '-') AS "EXTRA1"
	, ISNULL(B.EXTRA2, '-') AS "EXTRA2"
	, ISNULL(B.EXTRA4, '-') AS "EXTRA4"
#end
	, A.CYCLE AS "CYCLE"
	, CASE WHEN RESULT = 'NG' THEN 'NG' ELSE '-' END AS "NG"
	, C.COLOR AS "Color"
	, C.TIME AS "Time"
	, C.PERCENTAGE AS "Percent"
	, C.CNT AS "Count"
	, C.DEVIATION AS "Deviation"
	, CONVERT(VARCHAR(19), B.START_TIME, 120) AS "START_TIME"
	, CONVERT(VARCHAR(19), B.END_TIME, 120) AS "END_TIME"
	, CONVERT(VARCHAR(19), B.INSERT_DATE, 120) AS "INSERT_DATE"
	, CONVERT(VARCHAR(10), DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)/86400000) + 'Days ' +
	CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%86400000)/3600000) + 'h '+
	CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%3600000)/60000) + 'm '+
	CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%60000)/1000) + 's' AS "EXECUTION_TIME"
FROM
	SQE_TEST_CASE_MASTER A 
	LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
	LEFT OUTER JOIN SQE_VIEW_LATENCY_RESULT C ON A.TC_IDX = C.TC_IDX
WHERE 
	A.TC_IDX = :TC_IDX
ORDER BY 
	A.TC_IDX
				]]>
			</statement>
			<result mapper="org.anyframe.query.impl.jdbc.mapper.CallbackResultSetMapper"/>
		</query>
	</queries>
</queryservice>