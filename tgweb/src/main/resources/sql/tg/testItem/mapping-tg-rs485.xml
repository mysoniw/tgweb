<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE queryservice PUBLIC "-//ANYFRAME//DTD QUERYSERVICE//EN" 
	"http://www.anyframejava.org/dtd/anyframe-core-query-mapping-3.2.dtd">
<queryservice>
	<queries>
		<query id="Rs485MainDao.findAll" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT A.*
FROM (
	SELECT
		ROW_NUMBER() OVER(ORDER BY MAX(A.CATEGORY), MAX(A.MODEL), MAX(A.FW_VERSION) DESC) AS ROWNUM
		, COUNT(1) OVER() AS TOTALCOUNT
		, MAX(TC_IDX) AS TC_IDX
		, MAX(A.CATEGORY) AS CATEGORY
		, MAX(A.MODEL) AS MODEL
		, MAX(A.FW_VERSION) AS FW_VERSION
		, MAX(A.TEST_PROJECT_ID) AS TEST_PROJECT_ID
		, MAX(A.TEST_SUITE_ID) AS TEST_SUITE_ID
		, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
		, COUNT(TOTAL_CYCLE) AS TEST_CASE_COUNT
		, COUNT(TOTAL_CYCLE) - SUM(RESULT_INT) - SUM(NG_INT) AS TEST_CASE_PASS_COUNT
		, SUM(RESULT_INT) AS TEST_CASE_FAIL_COUNT
		, SUM(A.NG_INT) AS NG
		, CAST(ROUND((COUNT(TOTAL_CYCLE) - SUM(RESULT_INT) - SUM(NG_INT)) / CAST(COUNT(TOTAL_CYCLE) AS FLOAT) * 100, 2) AS VARCHAR) + '%' AS PASS_RATE
	FROM
		(
		SELECT
			MAX(TC_IDX) AS TC_IDX
			, MAX(A.CATEGORY) AS CATEGORY
			, MAX(A.MODEL) AS MODEL
			, MAX(A.FW_VERSION) AS FW_VERSION
			, MAX(A.TEST_PROJECT_ID) AS TEST_PROJECT_ID
			, MAX(A.TEST_SUITE_ID) AS TEST_SUITE_ID
			, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
			, CASE  
				WHEN SUM(A.RESULT_INT) > 0 THEN 1
				ELSE 0
			END	AS RESULT_INT
			, CASE 
				WHEN (SUM(A.RESULT_INT) = 0 OR SUM(A.RESULT_INT) IS NULL) AND SUM(A.NG_INT) > 0 THEN 1
				ELSE 0
			END AS NG_INT
			, COUNT(A.TEST_CASE_ID) AS TOTAL_CYCLE
		FROM
			(
			SELECT
				A.TC_IDX
				, A.CATEGORY
				, A.MODEL
				, A.FW_VERSION
				, CASE RESULT 
					WHEN 'PASS' THEN 0
					WHEN 'FAIL' THEN 1
					ELSE 0
				END AS RESULT_INT
				, CASE RESULT 
					WHEN 'NG' THEN 1
					ELSE 0
				END AS NG_INT
				, TEST_PROJECT_ID
				, TEST_SUITE_ID
				, TEST_CASE_ID
				, TEST_CASE_NAME
				, SUITE_DESCRIPTION
			FROM
				SQE_TEST_CASE_MASTER A LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
			WHERE 
				TEST_PROJECT_ID = 'RS_485'
				AND B.RESULT <> 'Unknown'
			#if ($CATEGORY && $CATEGORY != "" && $CATEGORY != "null")
				AND A.CATEGORY = :CATEGORY
			#end
			#if ($MODEL && $MODEL != "" && $MODEL != "null")
				AND A.MODEL = :MODEL
			#end
			#if ($FW_VERSION && $FW_VERSION != "" && $FW_VERSION != "null")
				AND A.FW_VERSION = :FW_VERSION
			#end
			#if ($TEST_SUITE_ID && $TEST_SUITE_ID != "" && $TEST_SUITE_ID != "null")
				AND A.TEST_SUITE_ID = :TEST_SUITE_ID
			#end
		) A
		WHERE 
			1 = 1
		GROUP BY
			A.CATEGORY, A.MODEL, A.FW_VERSION, A.TEST_SUITE_ID, A.TEST_CASE_ID
	) A
	WHERE
		1 = 1
	GROUP BY
		A.CATEGORY, A.MODEL, A.FW_VERSION, A.TEST_SUITE_ID
) A
WHERE 
	1 = 1
#if (!$loadOnce || $loadOnce == "" || $loadOnce == "null")
	AND A.ROWNUM BETWEEN (:rows * (:page - 1)) + 1 AND (:rows * :page)
#end
ORDER BY "{{sidx}}"
#if ($sord == "desc")
	DESC
#else
	ASC
#end
				]]>
			</statement>
		</query>
		<query id="Rs485MainDao.exportExcel" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT
	MAX(A.TEST_PROJECT_ID) AS "TEST_PROJECT_ID"
	, MAX(A.CATEGORY) AS "CATEGORY"
	, MAX(A.MODEL) AS "MODEL"
	, MAX(A.FW_VERSION) AS "FW_VERSION"
	, MAX(A.TEST_SUITE_ID) AS "TEST_SUITE_ID"
	, MAX(A.SUITE_DESCRIPTION) AS "SUITE_DESCRIPTION"
	, COUNT(TOTAL_CYCLE) AS "TOTAL_TC"
	, COUNT(TOTAL_CYCLE) - SUM(RESULT_INT) - SUM(NG_INT) AS "PASS"
	, SUM(RESULT_INT) AS "FAIL"
	, SUM(A.NG_INT) AS "NG"
	, CAST(ROUND((COUNT(TOTAL_CYCLE) - SUM(RESULT_INT) - SUM(NG_INT)) / CAST(COUNT(TOTAL_CYCLE) AS FLOAT) * 100, 2) AS VARCHAR) + '%' AS "PASS_RATE"
FROM
	(
	SELECT
		MAX(TC_IDX) AS TC_IDX
		, MAX(A.CATEGORY) AS CATEGORY
		, MAX(A.MODEL) AS MODEL
		, MAX(A.FW_VERSION) AS FW_VERSION
		, MAX(A.TEST_PROJECT_ID) AS TEST_PROJECT_ID
		, MAX(A.TEST_SUITE_ID) AS TEST_SUITE_ID
		, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
		, CASE  
			WHEN SUM(A.RESULT_INT) > 0 THEN 1
			ELSE 0
		END	AS RESULT_INT
		, CASE 
			WHEN (SUM(A.RESULT_INT) = 0 OR SUM(A.RESULT_INT) IS NULL) AND SUM(A.NG_INT) > 0 THEN 1
			ELSE 0
		END AS NG_INT
		, COUNT(A.TEST_CASE_ID) AS TOTAL_CYCLE
	FROM
		(
		SELECT
			A.TC_IDX
			, A.CATEGORY
			, A.MODEL
			, A.FW_VERSION
			, CASE RESULT 
				WHEN 'PASS' THEN 0
				WHEN 'FAIL' THEN 1
				ELSE 0
			END AS RESULT_INT
			, CASE RESULT 
				WHEN 'NG' THEN 1
				ELSE 0
			END AS NG_INT
			, TEST_PROJECT_ID
			, TEST_SUITE_ID
			, TEST_CASE_ID
			, TEST_CASE_NAME
			, SUITE_DESCRIPTION
		FROM
			SQE_TEST_CASE_MASTER A LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
		WHERE 
			TEST_PROJECT_ID = 'RS_485'
			AND B.RESULT <> 'Unknown'
		#if ($CATEGORY && $CATEGORY != "" && $CATEGORY != "null")
			AND A.CATEGORY = :CATEGORY
		#end
		#if ($MODEL && $MODEL != "" && $MODEL != "null")
			AND A.MODEL = :MODEL
		#end
		#if ($FW_VERSION && $FW_VERSION != "" && $FW_VERSION != "null")
			AND A.FW_VERSION = :FW_VERSION
		#end
		#if ($TEST_SUITE_ID && $TEST_SUITE_ID != "" && $TEST_SUITE_ID != "null")
			AND A.TEST_SUITE_ID = :TEST_SUITE_ID
		#end
	) A
	WHERE 
		1 = 1
	GROUP BY
		A.CATEGORY, A.MODEL, A.FW_VERSION, A.TEST_SUITE_ID, A.TEST_CASE_ID
) A
WHERE
	1 = 1
GROUP BY
	A.CATEGORY, A.MODEL, A.FW_VERSION, A.TEST_SUITE_ID
ORDER BY 
	MAX(A.CATEGORY), MAX(A.MODEL), MAX(A.FW_VERSION) DESC
				]]>
			</statement>
		</query>
		<query id="Rs485TcDao.findAll" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT A.*
FROM (
	SELECT
		ROW_NUMBER() OVER(ORDER BY
			#if ($filterColumn && $filterColumn != "" && $filterColumn != "null")
				MAX(A.RESULT_INT) DESC ,
			#end 
				MAX(A.CATEGORY)
				, MAX(A.MODEL)
				, MAX(A.FW_VERSION) DESC
				, MAX(A.TEST_CASE_ID)
		) AS ROWNUM
		, COUNT(1) OVER() AS TOTALCOUNT
		, MAX(A.TC_IDX) AS TC_IDX
		, MAX(A.CATEGORY) AS CATEGORY
		, MAX(A.MODEL) AS MODEL
		, MAX(A.FW_VERSION) AS FW_VERSION
		, MAX(A.TEST_PROJECT_ID) AS TEST_PROJECT_ID
		, MAX(A.TEST_SUITE_ID) AS TEST_SUITE_ID
		, A.TEST_CASE_ID
		, MAX(A.TEST_CASE_NAME) AS TEST_CASE_NAME
		, MAX(A.SUITE_DESCRIPTION) AS SUITE_DESCRIPTION
		, CASE 
			WHEN SUM(A.RESULT_INT) > 0 THEN 'FAIL'
			WHEN (SUM(A.RESULT_INT) = 0 OR SUM(A.RESULT_INT) IS NULL) AND SUM(A.NG_INT) > 0 THEN 'NG'
			ELSE 'PASS' 
		END AS RESULT
		, COUNT(A.TEST_CASE_ID) - SUM(A.RESULT_INT) - SUM(A.NG_INT) AS PASS
		, SUM(A.RESULT_INT) AS FAIL
		, SUM(A.NG_INT) AS NG
		, COUNT(A.TEST_CASE_ID) AS TOTAL_CYCLE
		, CAST(ROUND((COUNT(A.TEST_CASE_ID) - SUM(A.RESULT_INT) - SUM(A.NG_INT)) / CAST(COUNT(A.TEST_CASE_ID) AS FLOAT) * 100, 2) AS VARCHAR) + '%' AS PASS_RATE
		, MAX(A.EXTRA1) AS EXTRA1
	FROM
		(
		SELECT
			A.TC_IDX
			, A.CATEGORY
			, A.MODEL
			, A.FW_VERSION
			, CASE RESULT 
				WHEN 'PASS' THEN 0
				WHEN 'FAIL' THEN 1
				ELSE 0
			END AS RESULT_INT
			, CASE RESULT 
				WHEN 'NG' THEN 1
				ELSE 0
			END AS NG_INT
			, TEST_PROJECT_ID
			, TEST_SUITE_ID
			, TEST_CASE_ID
			, TEST_CASE_NAME
			, SUITE_DESCRIPTION
			, EXTRA1
		FROM
			SQE_TEST_CASE_MASTER A LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
		WHERE 
			TEST_PROJECT_ID = 'RS_485'
			AND B.RESULT <> 'Unknown'
			AND A.CATEGORY = :CATEGORY
			AND A.MODEL = :MODEL
			AND A.FW_VERSION = :FW_VERSION
			AND A.TEST_SUITE_ID = :TEST_SUITE_ID
		#if ($EXTRA1 && $EXTRA1 != "" && $EXTRA1 != "null")
			AND B.EXTRA1 = :EXTRA1
		#end
	) A
	WHERE 
		1 = 1
	GROUP BY
		A.TEST_CASE_ID
) A
WHERE 
	1 = 1
#if (!$loadOnce || $loadOnce == "" || $loadOnce == "null")
	AND A.ROWNUM BETWEEN (:rows * (:page - 1)) + 1 AND (:rows * :page)
#end
ORDER BY "{{sidx}}"
#if ($sord == "desc")
	DESC
#else
	ASC
#end
				]]>
			</statement>
		</query>
		<query id="Rs485TcDao.exportExcel" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT
	MAX(A.TEST_PROJECT_ID) AS "TEST_PROJECT_ID"
	, MAX(A.CATEGORY) AS "CATEGORY"
	, MAX(A.MODEL) AS "MODEL"
	, MAX(A.FW_VERSION) AS "FW_VERSION"
	, MAX(A.TEST_SUITE_ID) AS "TEST_SUITE_ID"
	, MAX(A.TEST_CASE_NAME) AS "TEST_CASE_NAME"
	, MAX(A.SUITE_DESCRIPTION) AS "SUITE_DESCRIPTION"
	, MAX(A.EXTRA1) AS "EXTRA1"
	, COUNT(A.TEST_CASE_ID) AS "TOTAL_CYCLE"
	, CASE 
		WHEN SUM(A.RESULT_INT) > 0 THEN 'FAIL'
		WHEN (SUM(A.RESULT_INT) = 0 OR SUM(A.RESULT_INT) IS NULL) AND SUM(A.NG_INT) > 0 THEN 'NG'
		ELSE 'PASS' 
	END AS "RESULT"
	, COUNT(A.TEST_CASE_ID) - SUM(A.RESULT_INT) - SUM(A.NG_INT) AS "PASS"
	, SUM(A.RESULT_INT) AS "FAIL"
	, SUM(A.NG_INT) AS "NG"
	, CAST(ROUND((COUNT(A.TEST_CASE_ID) - SUM(A.RESULT_INT) - SUM(A.NG_INT)) / CAST(COUNT(A.TEST_CASE_ID) AS FLOAT) * 100, 2) AS VARCHAR) + '%' AS "PASS_RATE"
FROM
	(
	SELECT
		A.TC_IDX
		, A.CATEGORY
		, A.MODEL
		, A.FW_VERSION
		, CASE RESULT 
			WHEN 'PASS' THEN 0
			WHEN 'FAIL' THEN 1
			ELSE 0
		END AS RESULT_INT
		, CASE RESULT 
			WHEN 'NG' THEN 1
			ELSE 0
		END AS NG_INT
		, TEST_PROJECT_ID
		, TEST_SUITE_ID
		, TEST_CASE_ID
		, TEST_CASE_NAME
		, SUITE_DESCRIPTION
		, EXTRA1
	FROM
		SQE_TEST_CASE_MASTER A LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
	WHERE 
		TEST_PROJECT_ID = 'RS_485'
		AND B.RESULT <> 'Unknown'
		AND A.CATEGORY = :CATEGORY
		AND A.MODEL = :MODEL
		AND A.FW_VERSION = :FW_VERSION
	#if ($TEST_SUITE_ID && $TEST_SUITE_ID != "" && $TEST_SUITE_ID != "null")
		AND A.TEST_SUITE_ID = :TEST_SUITE_ID
	#end
	#if ($RESULT && $RESULT != "" && $RESULT != "null")
		AND B.RESULT = :RESULT
	#end
	#if ($EXTRA1 && $EXTRA1 != "" && $EXTRA1 != "null")
		AND B.EXTRA1 = :EXTRA1
	#end
) A
WHERE 
	1 = 1
GROUP BY
	A.TEST_CASE_ID
ORDER BY 
	MAX(A.CATEGORY), MAX(A.MODEL), MAX(A.FW_VERSION) DESC, MAX(A.TEST_CASE_ID)
				]]>
			</statement>
		</query>
		<query id="Rs485CycleDao.findAll" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT A.*
FROM (
	SELECT
		ROW_NUMBER() OVER(ORDER BY
			#if ($filterColumn && $filterColumn != "" && $filterColumn != "null")
				RESULT ASC ,
			#end 
				A.CYCLE
		) AS ROWNUM
		, COUNT(1) OVER() AS TOTALCOUNT
		, A.TC_IDX
		, A.CATEGORY
		, A.MODEL
		, A.FW_VERSION
		, A.CYCLE
		, A.NODE_MAC
		, A.NODE_IP
		, RESULT
		, TEST_PROJECT_ID
		, TEST_CASE_ID
		, TEST_CASE_NAME
		, SUITE_DESCRIPTION
		, EXTRA1
		, STUFF(ISNULL((SELECT ', ' + LOG_NAME FROM SQE_TC_LOG WHERE TC_IDX = A.TC_IDX GROUP BY LOG_NAME FOR XML PATH (''), TYPE).value('.','VARCHAR(MAX)'), ''), 1, 2, '') AS LOG_FILE
		, B.CAUSE  
	FROM
		SQE_TEST_CASE_MASTER A LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
	WHERE 
		TEST_PROJECT_ID = 'RS_485'
		AND B.RESULT <> 'Unknown'
		AND A.CATEGORY = :CATEGORY
		AND A.MODEL = :MODEL
		AND A.FW_VERSION = :FW_VERSION
		AND A.TEST_CASE_ID = :TEST_CASE_ID
) A
WHERE 
	1 = 1
#if (!$loadOnce || $loadOnce == "" || $loadOnce == "null")
	AND A.ROWNUM BETWEEN (:rows * (:page - 1)) + 1 AND (:rows * :page)
#end
ORDER BY "{{sidx}}"
#if ($sord == "desc")
	DESC
#else
	ASC
#end
				]]>
			</statement>
		</query>
		<query id="Rs485CycleDao.exportExcel" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT
	TEST_PROJECT_ID AS "TEST_PROJECT_ID"
	, A.CATEGORY AS "CATEGORY"
	, A.MODEL AS "MODEL"
	, A.FW_VERSION AS "FW_VERSION"
	, TEST_SUITE_ID AS "TEST_SUITE_ID"
	, TEST_CASE_NAME AS "TEST_CASE_NAME"
	, SUITE_DESCRIPTION AS "SUITE_DESCRIPTION"
	, A.NODE_MAC AS "NODE_MAC"
	, A.NODE_IP AS "NODE_IP"
	, EXTRA1 AS "EXTRA1"
	, A.CYCLE AS "CYCLE"
	, RESULT AS "RESULT"
FROM
	SQE_TEST_CASE_MASTER A LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
WHERE 
	TEST_PROJECT_ID = 'RS_485'
	AND B.RESULT <> 'Unknown'
	AND A.CATEGORY = :CATEGORY
	AND A.MODEL = :MODEL
	AND A.FW_VERSION = :FW_VERSION
	AND A.TEST_CASE_ID = :TEST_CASE_ID
#if ($RESULT && $RESULT != "" && $RESULT != "null")
	AND B.RESULT = :RESULT
#end
ORDER BY 
	A.CYCLE
				]]>
			</statement>
		</query>
		<query id="Rs485DetailDao.findAll" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT A.*
FROM (
	SELECT
		ROW_NUMBER() OVER(ORDER BY C.SEQ) AS ROWNUM
		, COUNT(1) OVER() AS TOTALCOUNT
		, C.SEQ
		, C.KEY_EVENT
		, C.KEY_RESULT
		, CONVERT(VARCHAR(19), B.START_TIME, 120) AS START_TIME
		, CONVERT(VARCHAR(19), B.END_TIME, 120) AS END_TIME
		, CONVERT(VARCHAR(19), B.INSERT_DATE, 120) AS INSERT_DATE
		, CONVERT(VARCHAR(10), DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)/86400000) + 'Days ' +
		CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%86400000)/3600000) + 'h '+
		CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%3600000)/60000) + 'm '+
		CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%60000)/1000) + 's' AS EXECUTION_TIME
	FROM
		SQE_TEST_CASE_MASTER A 
		LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
		LEFT OUTER JOIN SQE_485_RESULT C ON A.TC_IDX = C.TC_IDX
	WHERE 
		A.TC_IDX = :TC_IDX
) A
WHERE 
	1 = 1
#if (!$loadOnce || $loadOnce == "" || $loadOnce == "null")
	AND A.ROWNUM BETWEEN (:rows * (:page - 1)) + 1 AND (:rows * :page)
#end
ORDER BY "{{sidx}}"
#if ($sord == "desc")
	DESC
#else
	ASC
#end
				]]>
			</statement>
		</query>
		<query id="Rs485DetailDao.exportExcel" mappingStyle="none">
			<statement>
				<![CDATA[
SELECT
	TEST_PROJECT_ID AS "TEST_PROJECT_ID"
	, A.CATEGORY AS "CATEGORY"
	, A.MODEL AS "MODEL"
	, A.FW_VERSION AS "FW_VERSION"
	, TEST_SUITE_ID AS "TEST_SUITE_ID"
	, TEST_CASE_NAME AS "TEST_CASE_NAME"
	, SUITE_DESCRIPTION AS "SUITE_DESCRIPTION"
	, A.NODE_MAC AS "NODE_MAC"
	, A.NODE_IP AS "NODE_IP"
	, B.EXTRA1 AS "EXTRA1"
	, A.CYCLE AS "CYCLE"
	, RESULT AS "RESULT"
	, B.CAUSE AS "CAUSE"
	, C.SEQ AS "SEQ"
	, C.KEY_EVENT AS "Key Event"
	, C.KEY_RESULT AS "Key Result"
	, CONVERT(VARCHAR(19), B.START_TIME, 120) AS "START_TIME"
	, CONVERT(VARCHAR(19), B.END_TIME, 120) AS "END_TIME"
	, CONVERT(VARCHAR(19), B.INSERT_DATE, 120) AS "INSERT_DATE"
	, CONVERT(VARCHAR(10), DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)/86400000) + 'Days ' +
	CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%86400000)/3600000) + 'h '+
	CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%3600000)/60000) + 'm '+
	CONVERT(VARCHAR(10), (DATEDIFF(MILLISECOND, B.START_TIME, B.END_TIME)%60000)/1000) + 's' AS "EXECUTION_TIME"
FROM
	SQE_TEST_CASE_MASTER A 
	LEFT OUTER JOIN SQE_TC_RESULT B ON A.TC_IDX = B.TC_IDX
	LEFT OUTER JOIN SQE_485_RESULT C ON A.TC_IDX = C.TC_IDX
WHERE 
	A.TC_IDX = :TC_IDX
ORDER BY 
	C.SEQ
				]]>
			</statement>
		</query>
	</queries>
</queryservice>
